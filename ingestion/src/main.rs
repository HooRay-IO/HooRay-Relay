pub mod model;
pub mod services;

fn main() {
    // TODO Day 4: replace with Lambda + Axum entry point.
    // AppConfig will be constructed here via services::dynamodb::AppConfig::from_env()
    // and the DynamoDB client via services::dynamodb::build_dynamo_client().
}
