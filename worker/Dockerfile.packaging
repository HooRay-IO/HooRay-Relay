FROM debian:bookworm-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates libssl3 \
    && rm -rf /var/lib/apt/lists/*

COPY target/release/worker /usr/local/bin/worker

ENV RUST_LOG=info

CMD ["worker"]
